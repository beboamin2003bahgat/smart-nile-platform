services:

  app:
    build: .
    ports:
      - "8501:8501"

  db:
    image: postgres:16

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: smartnile

    ports:
      - "5432:5432"